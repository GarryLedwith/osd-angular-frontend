{
  "name": "create-booking-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda – POST /equipment/{id}/bookings",
  "main": "index.js",
  "scripts": {
    "test:unit": "jest __tests__/handler.unit.test.js --coverage",
    "test:integration": "jest __tests__/handler.integration.test.js --testTimeout=20000",
    "test": "npm run test:unit"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
