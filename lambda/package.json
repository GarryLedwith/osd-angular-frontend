{
  "name": "create-booking-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda – POST /equipment/{id}/bookings",
  "main": "index.js",
  "scripts": {
    "test": "jest __tests__/handler.unit.test.js --coverage"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
